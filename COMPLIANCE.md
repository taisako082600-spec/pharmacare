# ガイドライン準拠状況

対象:
- **医療情報システムの安全管理に関するガイドライン 第7.0版**（厚生労働省）— 確認日 2026年8月16日
- **医療情報を取り扱う情報システム・サービスの提供事業者における安全管理ガイドライン 第2.0版**
  （総務省・経済産業省、令和7年3月28日改定）— 確認日 **2026年8月19日**

> このファイルは「現時点で何が満たせていて、何が満たせていないか」を正直に記録するものです。
> 施設への導入提案時、未対応項目を隠さず説明できる状態を保つのが目的です。

---

## 適用関係の整理

| | 名宛人 | PharmaCareとの関係 |
|---|---|---|
| 厚労省ガイドライン第7.0版 | 医療機関等（**介護事業者・薬局を含む**、概説編2.1） | **利用施設**が遵守する側 |
| 事業者ガイドライン第2.0版（総務省・経産省） | 医療情報システム等を提供する事業者 | **PharmaCare（提供者）が直接の名宛人** |

概説編4.4に「基準を満たした事業者を選定」「必要に応じて2省ガイドラインの遵守状況を確認」とあるため、
**施設に導入してもらうには、提供側が実質的に満たしている必要があります。**

事業者ガイドライン2.2.2 の分類では、PharmaCare は**対象事業者A（SaaS事業者）**、
Google Cloud が**対象事業者B（PaaS・IaaS事業者）**にあたります。
施設との契約者は PharmaCare であるため、**提供するシステム・サービス全体の管理責任を負います。**

同ガイドライン4.1 が求める医療機関等への情報提供は、[SERVICE_DISCLOSURE.md](SERVICE_DISCLOSURE.md)
（サービス仕様適合開示書）にまとめています。

---

## 事業者ガイドライン第2.0版 との突き合わせ（2026-08-19）

| 要求事項 | 状況 |
|---|---|
| 6.1 医療情報及びシステムが国内法の執行の及ぶ範囲にあること | **対応済**。Firestore・Cloud Run とも `asia-northeast1`（実機で確認） |
| 6.2 真正性 | **対応済**。監査ログに変更前後の値・作成者・サーバ時刻を記録。ルールで update/delete を禁止（追記のみ）<br>※ 患者情報の更新は監査ログを書いていなかったため、2026-08-19 に `patient_detail_screen.dart` の編集3経路（アレルギー／服薬上の特記事項／腎肝機能）へ追加 |
| 6.2 見読性 | **対応済**。2026-08-19 に書面出力（印刷）を実装（`lib/services/medical_record_print_service.dart`）。従来は画面表示のみで「書面を作成できるようにすること」を満たしていなかった |
| 6.2 保存性 | **対応済**。Point-in-Time Recovery 有効・削除保護 有効（実機で確認） |
| 6.3 電子署名 | **非該当**。法令で記名・押印が義務付けられた文書を作成していない |
| 6.4 取扱いに注意を要する文書等 | 薬剤服用歴に相当する記録を保持。アクセス制御・監査ログの対象 |
| 6.5 外部保存の要求事項 | 開示書で選定基準の確認に対応 |
| 4.1 医療機関等へ情報提供すべき項目 | **対応済**。[SERVICE_DISCLOSURE.md](SERVICE_DISCLOSURE.md) を作成 |
| 4.2 医療機関等との役割分担の明確化 | **対応済**。開示書§7に責任分界を記載 |
| 4.3 安全管理に係る評価（第三者・独立部門による） | **未実施** |
| 4.4 プライバシーマーク認定 または ISMS認証 | **未取得**。技術的対応では代替できず、費用と審査期間を要するため事業判断が必要 |
| 5.1.6 リスクコミュニケーション | 導入施設との定期的な情報共有の枠組みを整備中 |

---

## 対応済み

| 要件 | 対応内容 |
|---|---|
| 14.2 アクセス権限の管理（必要最小限） | `firestore.rules` で施設・ロール単位に分離。家族ロールは紐付く患者のみ参照可 |
| **14⑤ 二要素認証**（**2027年4月1日**が期限） | **TOTP（認証アプリ）方式で実装済み**。マイページ →「二要素認証」から各自で設定。SMS方式は採らない（下記） |
| 14.1.1 パスワード桁数・複雑性 | **13文字以上・英数字混在**を強制（`lib/services/password_policy.dart`）。テスト有り |
| 14.1.1 パスワードの定期変更 | ガイドラインが「不要」としているため、強制変更は設けていない |
| 17① アクセスログの記録 | ログイン・患者情報の参照/新規作成を `audit_logs` に記録 |
| 17② ログの改ざん・削除防止 | `firestore.rules` で `update`/`delete` を全面禁止。`userId` は認証済みUIDと一致必須 |
| 17③ 信頼できる時刻 | サーバー時刻（`request.time`）と一致しないログは書き込み拒否 |
| 13.3 通信の暗号化 | Firebase Hosting / Firestore / Cloud Run すべてTLS |
| **18① / 12.2 バックアップ**（数世代・複数方式） | **削除保護 + PITR（7日）+ 日次バックアップ（7世代）+ 週次バックアップ（14世代）**の3方式 |

### 二要素認証について

SMS方式ではなく **TOTP（Google Authenticator 等の認証アプリ）** を採用しています。

| | SMS方式 | TOTP方式（採用） |
|---|---|---|
| 費用 | 日本宛 約4.9円/通。MFA検証は月100回まで無料、超過9.8円/回 | **0円** |
| 電波 | 圏外の居室では届かず業務が止まる | 端末内で生成するため不要 |

10名が毎日ログインするとSMSでは月2,000円前後の継続費用になる一方、TOTPは無料です。
Identity Platform 側は TOTP のみ有効化し、SMS は無効のままにしてあります。

現在は**任意設定**です。全職員に必須化する場合は Identity Platform の `mfa.state` を
`MANDATORY` に変更します（既存利用者が締め出されないよう、周知と移行期間を設けてから）。
| 12⑥ / 12.3.2 離席時の自動画面ロックアウト | **15分間無操作で自動サインアウト**（`lib/services/idle_timeout.dart`）。介護施設は端末を共用するため、開いたまま離席されても他者に患者情報が見えない |
| 7.2.2 患者等への情報提供は参照のみ | 家族ロールは `firestore.rules` 上すべて `read` のみ。書き込み権限は一切付与していない |

---

## 未対応（重要度順）

### 1. ログイン試行の制限（14 遵守事項⑥(6)）

> 一定回数、認証に失敗した場合には、以降一定時間ログイン試行が不能となる仕組みを講じること。

アプリ側の実装はありません。Firebase Authにはサーバー側の自動スロットリングがありますが、
ガイドラインが想定する明示的なロックアウトとは異なります。**要確認事項。**

### 2. 監査ログのレビュー手段（17①「定期的にログを確認すること」）

ログの**記録**は実装しましたが、**閲覧UIがありません**。現状はFirestoreコンソールから直接見る必要があります。
また `firestore.rules` の読み取りは `isAdmin()` 限定のため、**施設側が自分でログを確認できません**。
施設が遵守義務を果たすには、施設単位のログ閲覧画面が必要です。

### 3. ID台帳・棚卸し（14 遵守事項⑧）

> 医療情報システムにおいて用いるIDについて、台帳管理等を行うほか、定期的に棚卸しを行い、
> 不要なものは適宜削除すること。

管理画面でユーザー一覧・削除は可能ですが、棚卸しの運用手順は未整備です。

### 4. 事業者としての開示文書（概説編4.5）

MDS/SDS（製造業者/サービス事業者による医療情報セキュリティ開示書）や
「サービス仕様適合開示書」の提供が求められますが、未作成です。

### 5. 家族向け「次回受診予定」の表示（機能を一時停止中）

`events` コレクションが `facilityId` と `type` しか持たず **`patientId` を持たない**ため、
家族に見せると同じ施設の他の入居者の受診予定（タイトルに氏名が入りうる）まで表示されてしまいます。
要配慮個人情報の目的外提供にあたるため、**セクションごと表示を停止しました**（2026-08-17）。

復活させるには `events` に `patientId` を持たせ、Firestoreルールで
`isLinkedFamily(patientId)` により患者単位に絞る必要があります。

---

## 2026-08-17 の見直しで見つけて直した不具合

ガイドラインと実装を突き合わせる過程で、コンプライアンス以前の**実バグ**も見つかりました。

| 内容 | 影響 |
|---|---|
| 監査ログが3重に機能していなかった（呼び出しが死んだコード内のみ／ルールが全書き込み禁止／失敗を握りつぶし） | 本番の `audit_logs` は0件だった。READMEには「監査ログを記録」と書かれており事実と異なっていた |
| 家族画面の「現在の処方」で薬剤名が1件も出ない | `patients/{id}/visits/{vid}/medicines` に家族の read 権限が無かった。ルールに `isLinkedFamily` を追加して解消 |
| 家族向け「次回受診予定」が常に読み込み失敗 | 上記7のとおり、権限を開けると別患者の情報が漏れるため、表示を停止する形で対応 |
| `flutter test` が常に失敗 | Flutter初期テンプレートの `widget_test.dart`（存在しないカウンターアプリを検証）が残っていた。削除してテストが信号として機能する状態に |

---

## 記録される監査ログ

| 操作 | action | 記録箇所 |
|---|---|---|
| ログイン | `LOGIN` | `login_screen.dart` |
| 患者詳細画面の表示（医療情報の参照） | `READ` | `patient_detail_screen.dart` |
| 入居者の新規登録 | `CREATE` | `patient_add_screen.dart` |

> バイタル・服薬記録など個別項目の更新は、まだ記録対象に含めていません。
> `patient_detail_screen.dart` が4,000行超あり、保存処理が多数に分散しているためです。
> ファイル分割（改善課題ストック B-1）と併せて対応するのが現実的です。
